-- LazySpec (plugin specification)
-- return {
local plugins = { 'dasupradyumna/midnight.nvim', lazy = false, priority = 1000 }
return plugins
-- `lazy` and `priority` are only needed if this is your primary colorscheme to load it first
-- }
